import { PlayerModel } from './player.model';

export const PLAYERS: PlayerModel[] = [
  new PlayerModel('Sean', 'McDermott', 0, []),
  new PlayerModel('Abigail', 'Wallace', 0, []),
  new PlayerModel('Walt', 'Whitman', 0, [])
]
